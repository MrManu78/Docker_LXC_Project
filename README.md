# Docker_LXC_Project
```bash
# Construction de l'image web à partir du fichier Dockerfile-Web, Nginx et Database ( se situer dans le dossier Dockerfile pour l'exécution de la commande)
docker build -f Dockerfile-Web .
docker build -f Dockerfile-Database .
docker build -f Dockefile-Nginx .

```
